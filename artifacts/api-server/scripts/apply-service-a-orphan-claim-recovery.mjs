import { readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import { fileURLToPath } from "node:url";

const here = path.dirname(fileURLToPath(import.meta.url));
const target = path.resolve(here, "../src/lib/tradeStore.ts");
let source = await readFile(target, "utf8");

const before = `      const claim = await tx.execute(sql\`\n        INSERT INTO eth_martingale_claims (generation, ticker, eastern_date, claimed_at_ms, client_order_id)\n        VALUES (\${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}, \${params.ticker}, \${params.easternDate}, \${now}, \${params.clientOrderId})\n        ON CONFLICT (generation, ticker) DO NOTHING RETURNING ticker\`);\n      if ((claim as unknown as { rows: unknown[] }).rows.length !== 1) {\n        // A proof-fence insert may have preceded this conflict. Roll it back\n        // with the rest of the transaction so no orderless attempt is consumed.\n        throw new EthMartingaleReservationRollback(\"ETH martingale ticker claim unavailable\");\n      }`;

const after = `      let claim = await tx.execute(sql\`\n        INSERT INTO eth_martingale_claims (generation, ticker, eastern_date, claimed_at_ms, client_order_id)\n        VALUES (\${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}, \${params.ticker}, \${params.easternDate}, \${now}, \${params.clientOrderId})\n        ON CONFLICT (generation, ticker) DO NOTHING RETURNING ticker\`);\n      if ((claim as unknown as { rows: unknown[] }).rows.length !== 1) {\n        // A prior crash/restart may have durably left only the ticker claim even\n        // though the transaction that owns the executable A order never exists.\n        // Recover only that exact orphan shape. A real/ambiguous A order, a claim\n        // owned by another road/legacy id, or a very fresh claim remains fail-closed.\n        const ownerResult = await tx.execute(sql\`\n          SELECT client_order_id, claimed_at_ms\n          FROM eth_martingale_claims\n          WHERE generation = \${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}\n            AND ticker = \${params.ticker}\n          FOR UPDATE\`);\n        const owner = (ownerResult as unknown as { rows: Array<Record<string, unknown>> }).rows[0];\n        const ownerClientOrderId = owner?.[\"client_order_id\"];\n        const ownerClaimedAtMs = Number(owner?.[\"claimed_at_ms\"]);\n        const serviceAClientId = typeof ownerClientOrderId === \"string\"\n          && /^eth-(?:yes|no)-[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(ownerClientOrderId);\n        const staleEnough = Number.isSafeInteger(ownerClaimedAtMs) && ownerClaimedAtMs <= now - 10_000;\n        const orderResult = await tx.execute(sql\`\n          SELECT id\n          FROM eth_martingale_orders\n          WHERE generation = \${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}\n            AND ticker = \${params.ticker}\n          LIMIT 1\`);\n        const noServiceAOrder = (orderResult as unknown as { rows: unknown[] }).rows.length === 0;\n\n        if (serviceAClientId && staleEnough && noServiceAOrder) {\n          const released = await tx.execute(sql\`\n            DELETE FROM eth_martingale_claims\n            WHERE generation = \${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}\n              AND ticker = \${params.ticker}\n              AND client_order_id = \${ownerClientOrderId}\n              AND NOT EXISTS (\n                SELECT 1 FROM eth_martingale_orders\n                WHERE generation = \${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}\n                  AND ticker = \${params.ticker}\n              )\n            RETURNING ticker\`);\n          if ((released as unknown as { rows: unknown[] }).rows.length === 1) {\n            claim = await tx.execute(sql\`\n              INSERT INTO eth_martingale_claims (generation, ticker, eastern_date, claimed_at_ms, client_order_id)\n              VALUES (\${ETH_MARTINGALE_ACTIVE_GENERATION_KEY}, \${params.ticker}, \${params.easternDate}, \${now}, \${params.clientOrderId})\n              ON CONFLICT (generation, ticker) DO NOTHING RETURNING ticker\`);\n            if ((claim as unknown as { rows: unknown[] }).rows.length === 1) {\n              logger.warn({\n                ticker: params.ticker,\n                priorClientOrderId: ownerClientOrderId,\n                replacementClientOrderId: params.clientOrderId,\n                priorClaimedAtMs: ownerClaimedAtMs,\n              }, \"Recovered orphan Service A ticker claim after proving no A ledger row\");\n            }\n          }\n        }\n\n        if ((claim as unknown as { rows: unknown[] }).rows.length !== 1) {\n          // A proof-fence insert may have preceded this conflict. Roll it back\n          // with the rest of the transaction so no orderless attempt is consumed.\n          throw new EthMartingaleReservationRollback(\"ETH martingale ticker claim unavailable\");\n        }\n      }`;

const count = source.split(before).length - 1;
if (count === 0 && source.includes("A definitive zero-fill insufficient-balance rejection must not strand")) {
  console.log("Service A orphan-claim recovery skipped: claim recovery is already implemented directly in tradeStore.ts");
} else {
  if (count !== 1) throw new Error(`Service A orphan-claim recovery: expected one claim anchor, found ${count}`);
  source = source.replace(before, after);
  await writeFile(target, source, "utf8");
  console.log("Service A orphan-claim recovery applied: only stale A-shaped claims with no A ledger row may be atomically reclaimed");
}
