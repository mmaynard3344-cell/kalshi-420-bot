import { logger } from "../logger.js";
import type { Eth420CandidateMarket } from "./eth420SixStepCandidate.js";
import { prepareEthDownfadeServiceIntent } from "./ethDownfadeServiceRuntime.js";
import { ETH_DOWNFADE_CONFIG, isEthDownfadeRole, type EthDownfadeRole } from "./ethDownfadeSignal.js";
import { createEthBigBetKalshiSubmitter } from "./ethBigBetKalshiExchange.js";
import { submitEthBigBetWithLongReversalAdmission } from "./ethLongReversalBigBetBridge.js";
import { ethDownfadeExecutionStore, initEthDownfadeExecutionStore } from "./ethDownfadeExecutionStore.js";
import { ethBigBetCapitalRiskCents } from "./ethBigBetLifecycle.js";
import { evaluateEthAccountCapital } from "./ethAccountCapitalGuard.js";
import { readApprovedEthBigBetCapitalBase } from "./ethBigBetApprovedCapitalProvider.js";
import { currentEthServiceEnablement } from "./ethServiceEnablementContract.js";
import { currentEthServiceRole } from "./ethServiceRole.js";
import { scheduleEthSignalEvidence } from "./ethSignalEvidenceLedger.js";
import { bkCapitalTelemetry, evaluateBkCapitalAdmission, isBkFreshBalanceCapitalPolicyEnabled, readBkFreshSameShardBalance } from "./bkFreshBalanceCapitalPolicy.js";
export const ETH_DOWNFADE_SERVICE_EXECUTION_APPROVED=true;
export type EthDownfadeLiveOutcome="disabled"|"no_signal"|"capital_unavailable"|"capital_blocked"|"routing_unavailable"|"storage_unavailable"|"submitted"|"blocked_duplicate"|"blocked_invalid_size"|"reservation_failed"|"submission_unknown"|"rejected"|"correlated_cap_unavailable"|"correlated_cap_blocked";
let storeReady:Promise<void>|null=null;async function ensureStoreReady(){if(!storeReady){storeReady=initEthDownfadeExecutionStore().catch(err=>{storeReady=null;throw err;});}return storeReady;}
export function isEthDownfadeServiceExecutionPermitted(role=currentEthServiceRole()):role is EthDownfadeRole{const enablement=currentEthServiceEnablement();return ETH_DOWNFADE_SERVICE_EXECUTION_APPROVED&&isEthDownfadeRole(role)&&enablement.valid&&enablement.mode==="downfade_live_requested"&&process.env["ETH_DOWNFADE_SERVICE_LIVE_ENABLED"]==="true";}
export async function runEthDownfadeServiceWhenExplicitlyEnabled(input:{market:Eth420CandidateMarket;exchangeIndex:number|null|undefined;}):Promise<EthDownfadeLiveOutcome>{const role=currentEthServiceRole();let currentMove:number|null=null,direction:string|null=null,p80:number|null=null,p90:number|null=null,p95:number|null=null,p99:number|null=null,validObservationCount=0;let signalRejectionReason:string|null=null;const finish=<T extends EthDownfadeLiveOutcome>(outcome:T,rejectionReason:string|null=null):T=>{const reason=rejectionReason??signalRejectionReason;logger.info({serviceRole:role,wagerCents:isEthDownfadeRole(role)?ETH_DOWNFADE_CONFIG[role].wagerCents:null,ticker:input.market.ticker,currentMove,direction,p80,p90,p95,p99,validObservationCount,outcome,rejectionReason:reason},"ETH Downfade evaluation");scheduleEthSignalEvidence({serviceRole:String(role),ticker:input.market.ticker,marketOpenTimeMs:input.market.openTimeMs,observedAtMs:input.market.observedAtMs,currentFloorStrike:input.market.floorStrike,currentMove,direction,p80,p90,p95,p99,sampleCount:validObservationCount,rejectionReason:reason,outcome});return outcome;};if(!isEthDownfadeServiceExecutionPermitted(role))return finish("disabled","execution_not_permitted");const intent=await prepareEthDownfadeServiceIntent({role,market:input.market,onEvaluation:o=>{currentMove=o.currentMove;direction=o.direction;p80=o.p80;p90=o.p90;p95=o.p95;p99=o.p99;validObservationCount=o.validObservationCount;signalRejectionReason=o.rejectionReason;}});if(!intent)return finish("no_signal");if(input.exchangeIndex==null||!Number.isInteger(input.exchangeIndex)||input.exchangeIndex<0)return finish("routing_unavailable","invalid_exchange_index");try{await ensureStoreReady();}catch{return finish("storage_unavailable","execution_store_unavailable");}const requestedRiskCents=ethBigBetCapitalRiskCents(intent.wagerCents,intent.limitPriceCents);if(requestedRiskCents<1)return finish("capital_unavailable","invalid_requested_risk");const flagEnabled=isBkFreshBalanceCapitalPolicyEnabled();const capitalBase=flagEnabled?null:await readApprovedEthBigBetCapitalBase(input.exchangeIndex);if(!flagEnabled&&!capitalBase)return finish("capital_unavailable","capital_base_unavailable");const oldCapital=capitalBase?evaluateEthAccountCapital({...capitalBase,requestedRiskCents}):null;const freshAvailableBalanceCents=flagEnabled?await readBkFreshSameShardBalance(input.exchangeIndex):capitalBase!.availableBalanceCents;const service=role==="downfade_e"?"E":"F";const admission=evaluateBkCapitalAdmission({service,ticker:input.market.ticker,exchangeIndex:input.exchangeIndex,requestedRiskCents,freshAvailableBalanceCents,oldPolicyDecision:flagEnabled?"unavailable":oldCapital!.allowed?"allow":oldCapital!.reason==="invalid_input"?"unavailable":"block",oldPolicyBlocker:flagEnabled?"not_evaluated_flagged_fresh_balance_policy":oldCapital!.allowed?null:oldCapital!.reason});if(!admission.finalAllowed){logger.info(bkCapitalTelemetry(admission,"not_attempted"),"BK capital admission");return finish(admission.finalDecision==="unavailable"?"capital_unavailable":"capital_blocked",admission.flagEnabled?`bk_fresh_balance_${admission.newPolicyDecision}`:(oldCapital!.allowed?null:oldCapital!.reason));}const exchange=createEthBigBetKalshiSubmitter(input.exchangeIndex);if(!exchange)return finish("routing_unavailable","exchange_route_unavailable");const executionCapital=flagEnabled?{availableBalanceCents:freshAvailableBalanceCents!,martingaleReserveCents:0,safetyReserveCents:0,otherBigBetReservedCents:0}:capitalBase!;const outcome=service==="E"?await submitEthBigBetWithLongReversalAdmission({service:"E",intent,exchangeIndex:input.exchangeIndex,executionStore:ethDownfadeExecutionStore,exchange,capital:executionCapital,requestedRiskCents}):await import("./ethBigBetExecutor.js").then(({submitEthBigBetIntent})=>submitEthBigBetIntent({intent,store:ethDownfadeExecutionStore,exchange,capital:executionCapital,requestedRiskCents}));logger.info(bkCapitalTelemetry(admission,outcome),"BK capital admission");const reason=outcome==="submitted"?null:outcome==="correlated_cap_unavailable"?"long_reversal_cap_unavailable":outcome==="correlated_cap_blocked"?"long_reversal_cap_blocked":outcome==="blocked_duplicate"?"duplicate_strategy_market":outcome==="blocked_invalid_size"?"invalid_order_size":outcome==="capital_blocked"?"capital_guard_blocked":outcome==="reservation_failed"?"durable_reservation_failed":outcome==="submission_unknown"?"exchange_submission_unknown":outcome==="rejected"?"exchange_rejected_reason_not_exposed_by_executor":outcome;return finish(outcome,reason);}
